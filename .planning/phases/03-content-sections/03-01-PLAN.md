---
phase: 03-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/content.ts
  - src/components/sections/AboutSection.tsx
  - src/components/sections/EducationSection.tsx
  - src/components/sections/SkillsSection.tsx
  - src/components/sections/ContactSection.tsx
autonomous: true
requirements: [ABOUT-01, ABOUT-02, ABOUT-03, EDU-01, EDU-02, EDU-03, SKILLS-01, SKILLS-02, SKILLS-03, CONTACT-01, CONTACT-02]

must_haves:
  truths:
    - "About section displays bio text mentioning Brown University, Math & CS, Visual Computing focus"
    - "About section has a styled photo placeholder with motif-corners class"
    - "About section contains inline anchor links to other portfolio sections (href='#experience', etc.)"
    - "Education section shows Brown University, 2023-2027, dual concentration, GPA 3.94"
    - "Education section lists coursework for both Math and CS concentrations"
    - "Education section shows TA role for CSCI 0200 Data Structures & Algorithms"
    - "Skills section displays six categorized tag groups"
    - "Skills categories are: Languages, Vision/Graphics, ML/AI, Software Engineering, Data, Teamwork/Teaching"
    - "Contact section has a clickable mailto:yali_sommer@brown.edu link"
    - "Contact section has a LinkedIn link that opens in new tab (target=_blank)"
  artifacts:
    - path: "src/data/content.ts"
      provides: "All portfolio section data (experience, education, projects, skills, research, contact)"
      exports: ["experienceEntries", "educationData", "skillGroups", "projects", "researchEntries", "contactLinks"]
    - path: "src/components/sections/AboutSection.tsx"
      provides: "About Me section component"
      exports: ["default"]
    - path: "src/components/sections/EducationSection.tsx"
      provides: "Education section component"
      exports: ["default"]
    - path: "src/components/sections/SkillsSection.tsx"
      provides: "Skills section component"
      exports: ["default"]
    - path: "src/components/sections/ContactSection.tsx"
      provides: "Contact section component"
      exports: ["default"]
  key_links:
    - from: "src/components/sections/AboutSection.tsx"
      to: "src/styles/tokens.ts"
      via: "import { DS }"
      pattern: "import.*DS.*from.*tokens"
    - from: "src/components/sections/ContactSection.tsx"
      to: "mailto:yali_sommer@brown.edu"
      via: "anchor href"
      pattern: "mailto:yali_sommer@brown.edu"
    - from: "src/components/sections/ContactSection.tsx"
      to: "linkedin.com/in/yalisommer"
      via: "anchor href with target=_blank"
      pattern: "target.*_blank"
---

<objective>
Create the shared content data file and four simpler section components: About, Education, Skills, and Contact.

Purpose: These four sections are structurally straightforward (no timeline, no expandable cards, no lab-style layout) and can be implemented together. The shared content data file centralizes all portfolio data so section components import from one source of truth.

Output: `src/data/content.ts` with all section data, plus four section component files under `src/components/sections/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-RESEARCH.md

@src/styles/tokens.ts
@src/styles/motifs.css
@src/components/Section.tsx
@src/App.tsx

<interfaces>
<!-- Design system tokens used by all section components -->
From src/styles/tokens.ts:
```typescript
export const DS = {
  bg: '#000000',
  surface: '#121212',
  border: 'rgba(255, 255, 255, 0.12)',
  textPrimary: '#e0e0e0',
  textSecondary: 'rgba(255, 255, 255, 0.55)',
  textMuted: 'rgba(255, 255, 255, 0.3)',
  accent: '#ffffff',
} as const
```

From src/components/Section.tsx:
```typescript
interface Props {
  id: string
  children: React.ReactNode
}
export default function Section({ id, children }: Props)
// Provides: scroll-reveal, 6rem vertical padding, 1200px max-width
// Sections are rendered inside <Section id="about"><AboutSection /></Section> in App.tsx
```

From src/styles/motifs.css:
```css
.motif-corners { /* Bounding-box corner brackets with green corners */ }
.motif-dots { /* Feature-point dot markers */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared content data file with all portfolio data</name>
  <files>src/data/content.ts</files>
  <read_first>
    - .planning/phases/03-content-sections/03-CONTEXT.md (decisions D-15 through D-24 for exact content)
    - ../portfolio-v5/src/components/HomePage.tsx (v5 project descriptions, tech tags, links)
  </read_first>
  <action>
Create `src/data/content.ts` exporting typed data objects for all 7 sections. This is the single source of truth for portfolio content.

**Interfaces to define:**

```typescript
export interface ExperienceEntry {
  company: string
  role: string
  period: string
  description: string
  upcoming?: boolean  // for SMBC entry
}

export interface EducationData {
  university: string
  location: string
  years: string
  degree: string
  gpa: string
  ta: { course: string; code: string }
  csCourses: string[]
  mathCourses: string[]
}

export interface SkillCategory {
  label: string
  items: string[]
}

export interface Project {
  id: string
  title: string
  tech: string[]
  description: string
  link: string | null
  image: string | null  // null = placeholder
}

export interface ResearchEntry {
  lab: string
  supervisor: string
  title: string
  description: string
  image: string | null  // null = placeholder
  status: string
}

export interface ContactLink {
  label: string
  value: string
  href: string
  external?: boolean  // true = target _blank
}
```

**Experience entries (per D-15 through D-18 verbatim):**

```typescript
export const experienceEntries: ExperienceEntry[] = [
  {
    company: 'SMBC',
    role: 'Data Strategy Intern, AI Team',
    period: 'Summer 2026',
    description: 'Incoming data strategy intern on SMBC\'s AI team. Working to rework internal systems to be AI-forward and integrate modern machine learning approaches across their data infrastructure.',
    upcoming: true,
  },
  {
    company: 'IturanTech',
    role: 'AI/ML Intern',
    period: 'Summer 2025',
    description: 'Honed an existing LSTM-based system to detect anomalies for vehicle theft detection across 1 million units. Researched and prototyped an LLM-based detection solution that was presented to the VP of Engineering and forwarded for further development after the internship concluded.',
  },
  {
    company: 'IturanTech',
    role: 'Data Science & Backend Intern',
    period: 'Spring 2025',
    description: 'Built the data pipelines used in the subsequent summer role. Trained ML models on ship movement anomaly detection data (South China Sea) as preparation for the summer\'s confidential telematics work. Gained hands-on experience with large-scale streaming data and early anomaly-detection algorithms.',
  },
  {
    company: 'GenWell',
    role: 'Product Management Intern',
    period: 'Spring 2025',
    description: 'Worked directly with the CEO at an early-stage wellness AI startup to help shape product direction. Led user interviews and synthesized insights into user personas that informed key product decisions. Contributed to feature definition, MVP scoping, and UI/UX design to align the experience with user needs.',
  },
]
```

**Education data (per D-19):**

```typescript
export const educationData: EducationData = {
  university: 'Brown University',
  location: 'Providence, RI',
  years: '2023 \u2013 2027',
  degree: 'B.Sc. Mathematics & Computer Science',
  gpa: '3.94',
  ta: { course: 'Data Structures & Algorithms', code: 'CSCI 0200' },
  csCourses: [
    'CSCI 1430 Computer Vision',
    'CSCI 1470 Deep Learning',
    'CSCI 2240 Interactive Computer Graphics',
    'CSCI 1951X Formal Proof & Verification',
    'CSCI 0200 Data Structures & Algorithms',
    'CSCI 0330 Introduction to Computer Systems',
    'CSCI 0320 Software Engineering',
    'CSCI 0220 Discrete Mathematics',
  ],
  mathCourses: [
    'MATH 1530 Abstract Algebra',
    'MATH 1410 Topology',
    'MATH 1260 Complex Analysis',
    'APMA 1690 Computational Probability',
    'APMA 1660 Statistical Inference',
    'MATH 0520 Linear Algebra',
    'MATH 0200 Multivariable Calculus',
  ],
}
```

Note: Use best judgment on the exact course list. The above is a representative set for a Brown Math+CS dual concentration student. Include courses that appeared in v5 and are plausible for the curriculum.

**Skill categories (per D-20, six groups):**

```typescript
export const skillGroups: SkillCategory[] = [
  { label: 'Languages', items: ['Python', 'TypeScript', 'C++', 'GLSL', 'Rust', 'SQL', 'Java'] },
  { label: 'Vision / Graphics', items: ['OpenCV', 'OpenGL', 'WebGL', 'CUDA', 'Three.js', 'Mitsuba 3', 'BlenderPy'] },
  { label: 'ML / AI', items: ['PyTorch', 'ONNX Runtime', 'YOLOv8', 'Hugging Face', 'scikit-learn', 'LSTMs'] },
  { label: 'Software Engineering', items: ['React', 'Node.js', 'Vite', 'Git', 'Docker', 'REST APIs', 'WebAssembly'] },
  { label: 'Data', items: ['PostgreSQL', 'Pandas', 'NumPy', 'Streaming Pipelines', 'AWS', 'Data Visualization'] },
  { label: 'Teamwork / Teaching', items: ['Technical Mentorship', 'User Research', 'Product Scoping', 'Code Review', 'TA Instruction'] },
]
```

**Projects (per D-21, five projects from v5):**

```typescript
export const projects: Project[] = [
  {
    id: 'violencenet',
    title: 'ViolenceNet',
    tech: ['Python', 'PyTorch', 'Computer Vision'],
    description: '3D CNN-based violence detection for automated security and content moderation.',
    link: 'https://github.com/yalisommer/ViolenceNet',
    image: null,
  },
  {
    id: 'realtime-renderer',
    title: 'Realtime Renderer',
    tech: ['C++', 'OpenGL', 'GLSL'],
    description: 'Extensive OpenGL-based real-time renderer with screen-space DoF, real-time shadow mapping, and more.',
    link: null,
    image: null,
  },
  {
    id: 'raytracer',
    title: 'Raytracer',
    tech: ['C++', 'Ray Tracing', 'Acceleration Structures'],
    description: 'Phong-based C++ raytracer with multi-bounce rays and anti-aliasing.',
    link: null,
    image: null,
  },
  {
    id: 'alma-metrics',
    title: 'Alma Metrics',
    tech: ['TypeScript', 'React', 'Node.js', 'ML'],
    description: 'ML-based prediction of college admissions trends.',
    link: null,
    image: null,
  },
  {
    id: 'confection',
    title: 'Confection',
    tech: ['JavaScript', 'Formal Methods', 'Forge'],
    description: 'Cellular automata epidemiological simulation with formal methods (Forge) for discovering interesting emergent cases.',
    link: 'https://github.com/yalisommer/Confection?tab=readme-ov-file',
    image: null,
  },
]
```

**Research entries (per D-22, D-23):**

```typescript
export const researchEntries: ResearchEntry[] = [
  {
    lab: 'Brown Visual Computing Lab',
    supervisor: 'Prof. James Tompkin & Joel Salzman (PhD)',
    title: 'Catacaustics: Neural Reconstruction of Caustic Light on Curved Surfaces',
    description: 'Studying rendering and inverse optimization of mirror-reflection scenes. Builds synthetic scenes in BlenderPy and Mitsuba 3 featuring planar and parabolic mirrors. Develops inverse methods in PyTorch that recover mirror geometry (parabolic coefficient) via finite-difference gradients and MS-SSIM loss. Analyzes optimization behavior and robustness across scene configurations. Broader goal: reconstruct 3D scenes where geometry is only visible indirectly through curved reflective surfaces.',
    image: '/images/bvc_cubes_gt.png',
    status: 'In Progress',
  },
  {
    lab: 'University of Edinburgh — Geometry Processing',
    supervisor: 'Prof. Amir Vaxman',
    title: 'DROK-Inspired Mesh Manipulation with Training-Time Constraints',
    description: 'Extended work inspired by the DROK (Data-Free Reduced Order Kinematics) paper. Trains low-dimensional kinematics representations with additional geometric constraints such as quad planarity and piecewise edge stretch conditions. Applies classical penalty methods and Lagrangian training approaches. End goal: an interactive tool for architects to manipulate meshes within a defined constraint-space rather than post-correcting unconstrained deformations.',
    image: null,
    status: 'Completed',
  },
]
```

**Contact links (per D-24):**

```typescript
export const contactLinks: ContactLink[] = [
  { label: 'Email', value: 'yali_sommer@brown.edu', href: 'mailto:yali_sommer@brown.edu' },
  { label: 'LinkedIn', value: 'linkedin.com/in/yalisommer', href: 'https://linkedin.com/in/yalisommer', external: true },
  { label: 'GitHub', value: 'github.com/yalisommer', href: 'https://github.com/yalisommer', external: true },
]
```

All exports must be named exports. All interfaces must also be exported. The file must compile under `noUnusedLocals: true`.
  </action>
  <verify>
    <automated>cd /Users/yalisommer/Desktop/Non-Schoolwork/Personal-Coding-Projects/portfolio-v6 && npx tsc --noEmit src/data/content.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/data/content.ts exists and contains `export const experienceEntries`
    - src/data/content.ts contains `export const educationData`
    - src/data/content.ts contains `export const skillGroups`
    - src/data/content.ts contains `export const projects`
    - src/data/content.ts contains `export const researchEntries`
    - src/data/content.ts contains `export const contactLinks`
    - src/data/content.ts contains `export interface ExperienceEntry`
    - src/data/content.ts contains `export interface Project`
    - src/data/content.ts contains `company: 'SMBC'`
    - src/data/content.ts contains `company: 'IturanTech'` (appears twice)
    - src/data/content.ts contains `company: 'GenWell'`
    - src/data/content.ts contains `gpa: '3.94'`
    - src/data/content.ts contains `mailto:yali_sommer@brown.edu`
    - src/data/content.ts contains `linkedin.com/in/yalisommer`
    - src/data/content.ts contains `external: true` on LinkedIn and GitHub entries
    - src/data/content.ts contains `title: 'ViolenceNet'`
    - src/data/content.ts contains `title: 'Confection'`
    - src/data/content.ts contains all 6 skill category labels: Languages, Vision / Graphics, ML / AI, Software Engineering, Data, Teamwork / Teaching
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 7 section data types defined and exported. All real content from CONTEXT.md decisions D-15 through D-24 present with correct values. File compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create AboutSection, EducationSection, SkillsSection, and ContactSection components</name>
  <files>src/components/sections/AboutSection.tsx, src/components/sections/EducationSection.tsx, src/components/sections/SkillsSection.tsx, src/components/sections/ContactSection.tsx</files>
  <read_first>
    - src/data/content.ts (just created in Task 1 -- source of truth for data)
    - src/styles/tokens.ts (DS.* constants for inline styles)
    - src/styles/motifs.css (.motif-corners class for photo placeholder)
    - src/App.tsx (current stub implementations for layout reference -- labelStyle and sectionHeadingStyle patterns)
    - .planning/phases/03-content-sections/03-CONTEXT.md (decisions D-13, D-14, D-19, D-20, D-24)
  </read_first>
  <action>
Create four section component files under `src/components/sections/`. Each component is a `default export function` that renders section content using `DS.*` inline styles. These components do NOT wrap themselves in `<Section>` -- they are rendered as children of `<Section>` in App.tsx.

**Shared style patterns to replicate from existing App.tsx stubs:**

```typescript
const sectionHeadingStyle: React.CSSProperties = {
  fontFamily: "'JetBrains Mono', monospace",
  fontSize: '0.7rem',
  letterSpacing: '0.25em',
  textTransform: 'uppercase',
  color: DS.textMuted,
  marginBottom: '3rem',
  paddingBottom: '1rem',
  borderBottom: `1px solid ${DS.border}`,
}

const labelStyle: React.CSSProperties = {
  fontFamily: "'JetBrains Mono', monospace",
  fontSize: '0.65rem',
  letterSpacing: '0.2em',
  textTransform: 'uppercase',
  color: DS.textMuted,
  marginBottom: '0.5rem',
}
```

Each component file should define these locally (or extract them to a shared styles utility if preferred -- Claude's discretion). Import `DS` from `../../styles/tokens`.

**AboutSection.tsx (per D-13, D-14, ABOUT-01/02/03):**

- Section heading: "About"
- Two-column grid layout: left column = name, tagline, focus tags; right column = bio text paragraphs
- Name: "Yali Sommer" in large heading (2.5rem, fontWeight 700, DS.textPrimary)
- Subtitle: "Brown University . Math & CS . Class of 2027" in labelStyle
- Focus tags: ['ML Engineering', 'Computer Vision', 'Computer Graphics', 'Visual Computing'] -- styled as bordered inline tags per existing stub pattern
- Bio text: Adapt the existing stub text. The first paragraph should be technical/confident ("I build systems that see..."). Include inline anchor links (per D-13, ABOUT-03):
  - `<a href="#experience" style={{ color: DS.textSecondary, textDecoration: 'underline', textUnderlineOffset: '3px' }}>experience</a>`
  - Similarly link to `#projects`, `#research`, `#education`, `#skills`, `#contact` where natural in the prose
- Photo placeholder (per D-14, ABOUT-02): A `div` with `className="motif-corners"` styled as an aspect-ratio box (e.g., 3:4 portrait), background DS.surface, with centered text "Photo" in DS.textMuted. Place this in the left column above or below the name block. Exact placement at Claude's discretion.

**EducationSection.tsx (per D-19, EDU-01/02/03):**

- Section heading: "Education"
- Two-column grid: left column = university info, right column = coursework
- Left column:
  - "Brown University" (1.5rem heading, bold)
  - "Providence, RI . 2023 - 2027" in labelStyle
  - "B.Sc. Mathematics & Computer Science" (1rem, DS.textSecondary)
  - "GPA: 3.94" (0.9rem, DS.textPrimary, bold or accent weight)
  - TA badge/line: "Teaching Assistant -- CSCI 0200 Data Structures & Algorithms" in labelStyle with a subtle border or tag treatment
- Right column:
  - Two sub-columns or two stacked lists: "Computer Science" and "Mathematics" coursework
  - Import `educationData` from `../../data/content` and render `educationData.csCourses` and `educationData.mathCourses`
  - Each course rendered in JetBrains Mono, 0.7rem, DS.textSecondary, with bottom border separator (matching existing stub pattern)

**SkillsSection.tsx (per D-20, SKILLS-01/02/03):**

- Section heading: "Skills"
- Import `skillGroups` from `../../data/content`
- Layout: CSS grid with responsive columns. Use `gridTemplateColumns: 'repeat(3, 1fr)'` for 6 categories displayed as 2 rows of 3 (better visual balance than 1 row of 6 or 4 columns as in stub).
- Each group: category label in labelStyle, followed by a column of tag items
- Each tag item: styled as a bordered pill or simple text with bottom border (matching existing stub's pattern of `fontSize: '0.9rem', color: DS.textSecondary, borderBottom: 1px solid DS.border`)
- The 6 categories must be exactly: Languages, Vision / Graphics, ML / AI, Software Engineering, Data, Teamwork / Teaching (per SKILLS-02)

**ContactSection.tsx (per D-24, CONTACT-01/02):**

- Section heading: "Contact"
- Import `contactLinks` from `../../data/content`
- Two-column grid: left = contact intro text + link list, right = terminal-style block (preserve existing stub's `$ whoami` aesthetic)
- Contact intro: "Open to opportunities in ML engineering and computer vision research. Happy to chat about graphics, systems, or building things that run fast."
- Link list: For each contact link, render label (in labelStyle, 80px width) and value as an `<a>` tag
  - Email: `href="mailto:yali_sommer@brown.edu"` -- no target attribute (CONTACT-01)
  - LinkedIn: `href="https://linkedin.com/in/yalisommer"` with `target="_blank"` and `rel="noopener noreferrer"` (CONTACT-02)
  - GitHub: `href="https://github.com/yalisommer"` with `target="_blank"` and `rel="noopener noreferrer"`
- Link hover: `onMouseEnter` sets color to DS.textPrimary, `onMouseLeave` resets to DS.textSecondary (same pattern as existing stub)
- Terminal block (right column): Preserve the existing `$ whoami` / `$ cat .profile` aesthetic from the current App.tsx stub

All components must use `export default function ComponentName()` pattern. All must import `DS` from `'../../styles/tokens'`. No unused imports or locals (strict mode). Do NOT wrap content in `<Section>` -- these are children of Section in App.tsx.
  </action>
  <verify>
    <automated>cd /Users/yalisommer/Desktop/Non-Schoolwork/Personal-Coding-Projects/portfolio-v6 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/AboutSection.tsx exists and contains `export default function AboutSection`
    - src/components/sections/AboutSection.tsx contains `motif-corners` (photo placeholder class)
    - src/components/sections/AboutSection.tsx contains `href="#experience"` (inline section link per ABOUT-03)
    - src/components/sections/AboutSection.tsx contains `href="#projects"` (inline section link)
    - src/components/sections/EducationSection.tsx exists and contains `export default function EducationSection`
    - src/components/sections/EducationSection.tsx contains `3.94` (GPA per EDU-01)
    - src/components/sections/EducationSection.tsx contains `CSCI 0200` or `Data Structures & Algorithms` (TA role per EDU-03)
    - src/components/sections/EducationSection.tsx contains `csCourses` and `mathCourses` (separate course lists per EDU-02)
    - src/components/sections/SkillsSection.tsx exists and contains `export default function SkillsSection`
    - src/components/sections/SkillsSection.tsx contains import from `../../data/content`
    - src/components/sections/ContactSection.tsx exists and contains `export default function ContactSection`
    - src/components/sections/ContactSection.tsx contains `mailto:yali_sommer@brown.edu`
    - src/components/sections/ContactSection.tsx contains `target="_blank"` (LinkedIn link per CONTACT-02)
    - src/components/sections/ContactSection.tsx contains `linkedin.com/in/yalisommer`
    - All four files import DS from `../../styles/tokens`
    - TypeScript compiles without errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>Four section components render real content using DS.* tokens and motif CSS classes. About has photo placeholder and inline section links. Education shows GPA 3.94, dual coursework lists, and TA role. Skills shows 6 categorized tag groups. Contact has mailto email link and target=_blank LinkedIn link. All compile cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `src/data/content.ts` exports all 6 data constants and 6 interfaces
- All 4 section component files exist under `src/components/sections/`
- Each component uses `export default function`
- No `<Section>` wrapper inside any component (they are Section children)
</verification>

<success_criteria>
- Content data file is the single source of truth for all portfolio text and metadata
- Four simpler section components (About, Education, Skills, Contact) render real content
- All 11 requirement IDs (ABOUT-01/02/03, EDU-01/02/03, SKILLS-01/02/03, CONTACT-01/02) are addressed
- TypeScript strict mode compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-01-SUMMARY.md`
</output>

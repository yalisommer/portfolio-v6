---
phase: quick
plan: 260427-p3t
type: execute
wave: 1
depends_on: []
files_modified: [src/hooks/useMeshBackground.ts]
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "Wireframe meshes are clearly visible against the black background (opacity ~0.3, scale ~2.0)"
    - "9 distinct mesh shapes appear stratified across the scroll height"
    - "Mobius strip, hyperboloid, and Enneper surface render as recognizable wireframe forms"
    - "Existing physics, scroll-tracking, and camera behavior are unchanged"
  artifacts:
    - path: "src/hooks/useMeshBackground.ts"
      provides: "Enhanced mesh background with 9 mesh types and increased visibility"
      contains: "MESH_OPACITY = 0.3"
  key_links:
    - from: "buildFallbackGeometry"
      to: "MESH_CONFIGS"
      via: "index % variants.length mapping"
      pattern: "variants\\[index % variants\\.length\\]"
---

<objective>
Enhance mesh wireframe visibility by increasing opacity and scale, and add 3 new iconic graphics-paper mesh types (Mobius strip, hyperboloid, Enneper minimal surface) to the bouncing mesh background.

Purpose: Make the wireframe meshes more prominent as a visual design element and add mathematically interesting shapes that reinforce the computer-graphics identity of the portfolio.
Output: Updated `src/hooks/useMeshBackground.ts` with 9 mesh configs and higher visibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/hooks/useMeshBackground.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase wireframe visibility constants</name>
  <files>src/hooks/useMeshBackground.ts</files>
  <action>
Make two targeted constant changes in `src/hooks/useMeshBackground.ts`:

1. Change `MESH_OPACITY` from `0.12` to `0.3` (line 19).

2. Inside `addMeshToScene`, change the scale divisor from `1.5` to `2.0` (line 207: `const scale = 1.5 / bsRadius` becomes `const scale = 2.0 / bsRadius`). Also update the corresponding `radius` in the pushed object (line 232) from `1.5` to `2.0`, and the horizontal margin `hw` calculation (line 211) from `bounds.w / 2 - 1.5` to `bounds.w / 2 - 2.0`, so the bounding/collision radius stays consistent with the new visual scale.

Do NOT change wireframe mode, colors, velocity, rotation speeds, physics logic, scroll-tracking, or camera behavior.
  </action>
  <verify>
    <automated>cd /Users/yalisommer/Desktop/Non-Schoolwork/Personal-Coding-Projects/portfolio-v6 && grep -n "MESH_OPACITY = 0.3" src/hooks/useMeshBackground.ts && grep -n "const scale = 2.0 / bsRadius" src/hooks/useMeshBackground.ts && grep -n "radius: 2.0" src/hooks/useMeshBackground.ts</automated>
  </verify>
  <done>MESH_OPACITY is 0.3, mesh scale divisor is 2.0, radius is 2.0, and margin is 2.0 -- meshes are larger and more visible while physics stays consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Add Mobius strip, hyperboloid, and Enneper surface geometries plus 3 new MESH_CONFIGS</name>
  <files>src/hooks/useMeshBackground.ts</files>
  <action>
In `src/hooks/useMeshBackground.ts`, make these additions:

**A. Extend `MESH_CONFIGS` array** -- append 3 new entries after the existing 6:
```typescript
  { url: null, colorIndex: 0 },  // Mobius strip
  { url: null, colorIndex: 3 },  // Hyperboloid
  { url: null, colorIndex: 5 },  // Enneper surface
```

**B. Add 3 new geometry builder functions** before `buildFallbackGeometry`, then reference them in the `variants` array:

1. **`buildMobiusGeometry()`** -- returns `THREE.BufferGeometry`:
   - Parametric surface: u in [0, 2*PI] (96 segments), v in [-0.5, 0.5] (10 segments)
   - Equations: `x = (1 + v*cos(u/2)) * cos(u)`, `y = (1 + v*cos(u/2)) * sin(u)`, `z = v * sin(u/2)`
   - Build position array and index array for a triangle mesh grid (96 x 10 quads, 2 triangles each)
   - Set attribute 'position' as Float32BufferAttribute, set index, call computeVertexNormals()

2. **`buildHyperboloidGeometry()`** -- returns `THREE.BufferGeometry`:
   - Create a profile curve: 24 `THREE.Vector2` points sampling `t` from -1.2 to 1.2
   - For each point: `r = Math.sqrt(1 + t*t) * 0.8`, `y = t`
   - Return `new THREE.LatheGeometry(points, 48)`

3. **`buildEnneperGeometry()`** -- returns `THREE.BufferGeometry`:
   - Parametric surface: u, v in [-1.3, 1.3], 40x40 grid
   - Equations: `x = u - u^3/3 + u*v^2`, `y = v - v^3/3 + v*u^2`, `z = u^2 - v^2`
   - Build position array and index array for triangle mesh grid (40 x 40 quads, 2 triangles each)
   - Set attribute 'position' as Float32BufferAttribute, set index, call computeVertexNormals()

**C. Update `buildFallbackGeometry`** -- extend the `variants` array from 6 to 9 entries by appending:
```typescript
    () => buildMobiusGeometry(),
    () => buildHyperboloidGeometry(),
    () => buildEnneperGeometry(),
```

Update the comment above `buildFallbackGeometry` from "6 distinct" to "9 distinct".

The existing `index % variants.length` logic handles the mapping automatically. MESH_CONFIGS.length is now 9, so `bucketHeight = worldHeight / 9` -- stratification adjusts automatically.

Do NOT modify any physics, velocity, rotation, collision, scroll-tracking, or camera logic.
  </action>
  <verify>
    <automated>cd /Users/yalisommer/Desktop/Non-Schoolwork/Personal-Coding-Projects/portfolio-v6 && npx tsc --noEmit 2>&1 | head -20 && grep -c "url: null" src/hooks/useMeshBackground.ts && grep -n "buildMobiusGeometry\|buildHyperboloidGeometry\|buildEnneperGeometry" src/hooks/useMeshBackground.ts</automated>
  </verify>
  <done>TypeScript compiles with no errors. MESH_CONFIGS has 5 `url: null` entries (2 original + 3 new). Three new geometry builder functions exist and are referenced in the variants array. Total mesh count is 9 with automatic stratified layout.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `MESH_CONFIGS` has exactly 9 entries
3. `buildFallbackGeometry` variants array has exactly 9 entries
4. MESH_OPACITY is 0.3, scale divisor is 2.0
5. No changes to updatePhysics, animate, camera tracking, or scroll sync logic
</verification>

<success_criteria>
- TypeScript compiles cleanly
- 9 mesh shapes appear in the background when scrolling through portfolio sections
- Wireframes are clearly visible (opacity 0.3, scale 2.0)
- Mobius strip, hyperboloid, and Enneper surface are recognizable wireframe forms
- Existing bounce physics, scroll tracking, and camera behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260427-p3t-enhance-mesh-wireframe-visibility-and-ad/260427-p3t-SUMMARY.md`
</output>

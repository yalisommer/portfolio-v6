---
phase: quick
plan: 260414-u64
subsystem: ui
tags: [three.js, scroll, camera, mesh, hero, transition]

requires:
  - phase: 03-content-sections
    provides: MeshBackground component and useMeshBackground hook with bouncing physics
provides:
  - Scroll-driven Three.js camera that tracks page scroll position
  - Fixed Y mesh positioning replacing physics-based bouncing
  - Extended hero visibility threshold (0.3 -> 0.8) and content buffer (35vh -> 100vh)
affects: [mesh-background, hero-transition, content-layout]

tech-stack:
  added: []
  patterns: [scroll-driven-camera, fixed-mesh-placement]

key-files:
  created: []
  modified:
    - src/hooks/useMeshBackground.ts
    - src/App.tsx

key-decisions:
  - "Removed all physics (velocity, collision, DVD-bounce) in favor of scroll-driven camera -- meshes now feel page-embedded"
  - "Positive-only rotation speeds (no randomSign) -- subtle slow spin is sufficient without direction variation"

patterns-established:
  - "Scroll-driven camera: camera.position.y = -(contentScrollY / window.innerHeight) * bounds.h"

requirements-completed: []

duration: 1.5min
completed: 2026-04-14
---

# Quick Task 260414-u64: Fix Mesh Scroll Positioning and Hero Transition

**Scroll-driven Three.js camera replaces physics-bounce model; hero threshold extended to 0.8vh with 100vh content buffer**

## Performance

- **Duration:** 1.5 min
- **Started:** 2026-04-14T19:44:53Z
- **Completed:** 2026-04-14T19:46:20Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Replaced bouncing-physics model (velocity, DVD-bounce, elastic collisions) with scroll-driven camera positioning so meshes feel embedded in page content
- Meshes placed at fixed Y depths (-1.5h, -4.0h, -7.0h) and slowly rotate in place
- Hero text stays visible until 80% viewport scroll (up from 30%)
- Content sections pushed to 100vh scroll depth (up from 35vh), giving more aquarium viewing time

## Task Details

1. **Task 1: Convert mesh background from physics-bounce to scroll-driven camera** (refactor)
   - Removed `VELOCITY_MIN`, `VELOCITY_MAX` constants
   - Replaced `BouncingMesh` interface with `SceneMesh` (mesh + rotationSpeed only)
   - Deleted `randomSign()` and `updatePhysics()` functions entirely
   - Set mesh Y positions to fixed depths based on `colorIndex`
   - Animate loop now reads `window.scrollY` to position camera
   - Rotation speeds reduced to 0.01-0.03 range

2. **Task 2: Delay hero fade and push content sections deeper** (fix)
   - Hero fade threshold: `0.3` -> `0.8`
   - Content paddingTop: `35vh` -> `100vh`
   - Gradient height: `35vh` -> `100vh`
   - Updated comments to reflect new values

## Files Modified
- `src/hooks/useMeshBackground.ts` - Replaced physics model with scroll-driven camera, fixed mesh placement, slow rotation only
- `src/App.tsx` - Extended hero threshold to 0.8, content buffer to 100vh, gradient to 100vh

## Decisions Made
- Removed all physics (velocity, collision, DVD-bounce) in favor of scroll-driven camera -- meshes now feel page-embedded rather than floating independently
- Positive-only rotation speeds (no randomSign) -- subtle slow spin is sufficient without needing direction variation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Pre-existing `tsc -b` errors in `src/data/content.ts` (4 errors about `data.about` and `data.contact` being `unknown`). These are not caused by this task's changes and exist in the codebase before this task. `tsc --noEmit` passes clean for the files modified in this task.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None.

## Self-Check: PASSED

- [x] src/hooks/useMeshBackground.ts exists and contains SceneMesh interface
- [x] No physics code remains (VELOCITY, updatePhysics, BouncingMesh, randomSign all absent)
- [x] camera.position.y tracks scroll offset (line 210)
- [x] src/App.tsx hero threshold at 0.8, paddingTop at 100vh, gradient height at 100vh
- [x] SUMMARY.md created
- [x] tsc --noEmit passes clean

---
*Quick task: 260414-u64*
*Completed: 2026-04-14*
